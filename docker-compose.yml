version: '3.8'

services:
  app:
    # 直接拉取 GitHub Actions 在 Docker Hub 上构建好的镜像
    image: smallds2004/eyemanager-web:latest
    container_name: eyemanager-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      # 填写您的外部 PostgreSQL 数据库连接串
      DATABASE_URL: "${DATABASE_URL}"
